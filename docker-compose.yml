version: '3'

services:
  web:
    image: nginx:1.21.1
    ports: 
      - "8000:80"
    depends_on: 
      - app
    volumes: 
      - ./docker/web/default.conf:/etc/nginx/conf.d/default.conf
      - .:/var/www/html
  app:
    image: php:8.0.8-fpm
    volumes:
      - .:/var/www/html